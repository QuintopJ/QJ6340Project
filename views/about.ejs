<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= typeof title !== 'undefined' ? title : 'About · NFT Mint' %></title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"/>

  <!-- Site CSS from /public -->
  <link rel="stylesheet" href="/css/style.css" />

  <style>
    :root{
      --parallax-speed: 0.22;
      --neon-pink: #ff4fd8;
      --accent: #c87ac5;
    }

    html, body { height: 100%; margin: 0; background:#000; overflow-x:hidden; }

    /* Page wrapper */
    .about-page{
      position: relative;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      isolation: isolate;
    }

    /* Parallax background (served from /public/images) */
    .about-bg{
      position: fixed; inset:0;
      background: url("/images/q_about_bg.png") center / cover no-repeat;
      will-change: transform; z-index:-2; transform: translateY(0);
    }
    .about-tint{ position:fixed; inset:0; background:rgba(0,0,0,.38); z-index:-1; pointer-events:none; }

    /* Navbar */
    .navbar.bg-dark{ background-color: rgba(0,0,0,.72) !important; backdrop-filter: blur(6px); }

    /* Hero */
    .hero{
      color:#fff; text-align:center;
      padding: 6rem 1rem 2rem;
      background: rgba(0,0,0,.35); backdrop-filter: blur(6px);
      border-radius: 12px; margin: 0 auto 2rem; max-width: 960px;
    }
    .hero h1{ letter-spacing:.5px; }
    .tagline{ opacity:.9; }

    /* Avatar */
    .avatar{
      width: 132px; height:132px; object-fit:cover; border-radius: 50%;
      border: 2px solid rgba(255,255,255,.35);
      box-shadow: 0 0 18px rgba(200,122,197,.6), 0 0 28px rgba(120,200,255,.25);
    }

    /* Glass cards */
    .glass{
      background: rgba(17,17,17,.26);
      border: 1px solid rgba(200,122,197,.28);
      border-radius: 16px; color:#f5f5f7; backdrop-filter: blur(10px);
      box-shadow: 0 8px 24px rgba(0,0,0,.35);
      transition: border-color .25s, box-shadow .25s, transform .25s;
    }
    .glass:hover{ transform: translateY(-3px); border-color: var(--accent);
      box-shadow: 0 0 16px rgba(200,122,197,.6), 0 0 32px rgba(100,200,255,.35); }

    .neon-title{ color: var(--neon-pink); }

    .badge-tag{
      background: rgba(255,255,255,.08);
      border:1px solid rgba(255,255,255,.25);
      color:#fff; font-weight:500;
    }

    .cta .btn{ background: var(--accent); border-color: var(--accent); }

    footer.bg-dark{ background-color: rgba(0,0,0,.82) !important; margin-top:auto; backdrop-filter: blur(6px); }

    /* De-emphasize highlights accordion */
    .accordion-button{ background: transparent; color:#fff; }
    .accordion-button:focus{ box-shadow: none; }
    .accordion-item{ background: transparent; border-color: rgba(255,255,255,.2); }
    .accordion-button.collapsed{ opacity: .75; }
    .accordion-body{ color: rgba(255,255,255,.8); }

    /* Respect reduced motion */
    @media (prefers-reduced-motion: reduce){ .about-bg{ transform:none !important; } }
  </style>
</head>
<body>
  <div class="about-page">
    <!-- Parallax layers -->
    <div class="about-bg" aria-hidden="true"></div>
    <div class="about-tint" aria-hidden="true"></div>

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
      <div class="container">
        <a class="navbar-brand fw-bold text-primary" href="/">NFT Mint</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto align-items-center">
          <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="/projects">Projects</a></li>
          <li class="nav-item"><a class="nav-link" href="/about">About</a></li>
          <li class="nav-item"><a class="nav-link" href="/contact">Contact</a></li>

          <!-- Connect Wallet button -->
          <li class="nav-item ms-3">
            <button id="wallet-connect" class="btn btn-outline-info btn-sm fw-bold">
              🔗 Connect Wallet
            </button>
          </li>
        </ul>
      </div> <!-- /#navbarNav -->
    </div> <!-- /.container -->
  </nav> <!-- /.navbar -->

    <!-- Hero -->
    <header class="hero container">
      <img src="/images/qool_cat.png" alt="Artist avatar" class="avatar mb-3">
      <h1 class="display-6 fw-bold">Quinn Harris</h1>
      <p class="tagline lead mb-3">Digital artist exploring the intersection of <span class="neon-title">art</span>, <span class="neon-title">style</span>, and <span class="neon-title">blockchain</span>.</p>
      <div class="d-flex justify-content-center gap-2 flex-wrap">
        <span class="badge badge-tag">NFTs</span>
        <span class="badge badge-tag">Vaporwave</span>
        <span class="badge badge-tag">Streetwear Aesthetics</span>
        <span class="badge badge-tag">LA Nights</span>
      </div>
    </header>

    <!-- Main -->
    <main class="container pb-5">
      <div class="row g-4 align-items-stretch">
        <!-- Full-width About (bigger, front and center) -->
        <section class="col-12">
          <div class="p-4 p-md-5 glass h-100">
            <div class="row g-4">
              <div class="col-lg-7">
                <h2 class="h4 neon-title mb-3">About the Work</h2>
                <p>I create neon-soaked worlds that blend street culture with futurism. My process mixes sketching, digital illustration, and color-grading passes to push glow, grain, and motion. Each piece aims to feel like a moment from a story you walked into—palms, skyline, and that late-night hum of the city.</p>
                <p>On-chain, I’m focused on intentional drops—small collections and 1/1s where the art leads the tech. I’m also exploring utility around collector perks, token-gated sketches, and behind-the-scenes breakdowns.</p>
                <h3 class="h6 text-white-50 mt-4 mb-2">What I’m exploring now</h3>
                <ul class="mb-0">
                  <li>Motion studies with neon bloom & film grain</li>
                  <li>LA beach/night vibes with retro-futurist color</li>
                  <li>Collector-first drops with process unlocks</li>
                </ul>
              </div>
              <div class="col-lg-5">
                <div class="glass p-3 h-100">
                  <h3 class="h5 neon-title mb-3">Quick Facts</h3>
                  <ul class="list-unstyled mb-3">
                    <li class="mb-2">🎨 Palette: deep purple + electric pink (+ teal)</li>
                    <li class="mb-2">🌴 Muse: LA after dark & beach nights</li>
                    <li class="mb-2">🧪 Focus: motion, glow, grain</li>
                    <li class="mb-2">💬 Commissions: open</li>
                  </ul>
                  <div>
                    <span class="badge badge-tag me-1 mb-1">Photoshop</span>
                    <span class="badge badge-tag me-1 mb-1">Procreate</span>
                    <span class="badge badge-tag me-1 mb-1">Illustrator</span>
                    <span class="badge badge-tag me-1 mb-1">After Effects</span>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </section>

        <!-- Highlights (collapsed by default, less prominent) -->
        <section class="col-12">
          <div class="glass">
            <div class="accordion accordion-flush" id="highlights">
              <div class="accordion-item">
                <h2 class="accordion-header">
                  <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#hl">
                    <span class="neon-title fw-semibold me-2">Selected Highlights</span>
                    <span class="text-white-50">(open)</span>
                  </button>
                </h2>
                <div id="hl" class="accordion-collapse collapse" data-bs-parent="#highlights">
                  <div class="accordion-body">
                    <div class="row g-3">
                      <div class="col-12 col-md-4">
                        <div class="p-3 border rounded-3 border-opacity-25">
                          <h3 class="h6 mb-1">2025 · “Free & FLY — Genesis”</h3>
                          <p class="small mb-0 text-white-75">Collection drop exploring palms, synth skies, and city motion.</p>
                        </div>
                      </div>
                      <div class="col-12 col-md-4">
                        <div class="p-3 border rounded-3 border-opacity-25">
                          <h3 class="h6 mb-1">2024 · “Qool Cat — Night Run”</h3>
                          <p class="small mb-0 text-white-75">1/1 portrait series—vaporwave + noir energy.</p>
                        </div>
                      </div>
                      <div class="col-12 col-md-4">
                        <div class="p-3 border rounded-3 border-opacity-25">
                          <h3 class="h6 mb-1">2023 · Process Studies</h3>
                          <p class="small mb-0 text-white-75">Color, grain, and neon glow experiments that shaped the look.</p>
                        </div>
                      </div>
                    </div>
                  </div>
                </div> <!-- /collapse -->
              </div>
            </div>
          </div>
        </section>

        <!-- CTA -->
        <section class="col-12">
          <div class="p-4 glass text-center cta">
            <h2 class="h5 mb-2">Want to collaborate or commission a piece?</h2>
            <p class="text-white-50 mb-3">I’d love to hear your idea. Let’s make something bold.</p>
            <a class="btn btn-lg btn-primary" href="/contact">Contact Me</a>
            <p class="small mt-3 mb-0">Prefer email?
              <a class="text-decoration-none" style="color: var(--neon-pink);" href="mailto:QuinnsDesign@hotmail.com">QuinnsDesign@hotmail.com</a>
            </p>
          </div>
        </section>
      </div>
    </main>

    <!-- Footer -->
    <footer class="bg-dark text-white text-center py-3">
      <div class="container">
        <p class="mb-1">&copy; 2025 Quinn Harris · Personal NFT Mint</p>
        <a href="mailto:QuinnsDesign@hotmail.com" class="text-primary text-decoration-none">Email: QuinnsDesign@hotmail.com</a>
      </div>
    </footer>
  </div>

  <!-- Parallax -->
  <script>
    (function(){
      const bg = document.querySelector('.about-bg');
      const prefersReduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
      if (!bg || prefersReduced) return;
      const speed = parseFloat(getComputedStyle(document.documentElement).getPropertyValue('--parallax-speed')) || 0.22;
      let ticking = false;
      function onScroll(){
        if (ticking) return;
        ticking = true;
        requestAnimationFrame(() => {
          const y = window.scrollY * speed;
          bg.style.transform = `translateY(${y}px)`;
          ticking = false;
        });
      }
      window.addEventListener('scroll', onScroll, { passive:true });
      onScroll();
    })();
  </script>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

  <!-- Optional site JS (served from /public/js) -->
  <script type="module" src="/js/index.js"></script>
</body>
</html>
