<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>

  <title>
    <%= typeof title !== 'undefined'
        ? title
        : (featured ? `${featured.title} · Personal NFT Mint` : 'Personal NFT Mint') %>
  </title>

  <!-- Site CSS (served from /public) -->
  <link rel="stylesheet" href="/css/style.css" />

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>

<body>
  <!-- Header (Bootstrap Navbar) -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
    <div class="container">
      <a class="navbar-brand fw-bold text-primary" href="/">NFT Mint</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto align-items-center">
          <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="/projects">Projects</a></li>
          <li class="nav-item"><a class="nav-link" href="/about">About</a></li>
          <li class="nav-item"><a class="nav-link" href="/contact">Contact</a></li>

          <!-- Connect Wallet button -->
          <li class="nav-item ms-3">
            <button id="wallet-connect" class="btn btn-outline-info btn-sm fw-bold">
              🔗 Connect Wallet
            </button>
          </li>
        </ul>
      </div> <!-- /#navbarNav -->
    </div> <!-- /.container -->
  </nav> <!-- /.navbar -->

  <!-- Splash Section -->
  <section class="splash">
    <img
      src="/images/q_splash.png"
      alt="Live Free and Fly splash art"
      class="splash-img"
      loading="eager"
      width="1920" height="900"
    />
    <div class="splash-text">
      <h1>Live Free and FLY</h1>
      <p>Exploring NFTs through art and style.</p>
      <a href="/projects" class="btn btn-primary">View Projects</a>
    </div>
  </section>

  <!-- Featured Project -->
  <section class="featured container my-5">
    <h2 class="mb-3">Featured Project</h2>

    <% if (featured) { %>
      <div class="project-card d-flex align-items-stretch gap-3 p-3 border rounded-3">
        <div class="ratio ratio-16x9" style="max-width: 520px;">
          <img
            src="<%= featured.image || '/images/placeholder.png' %>"
            alt="<%= featured.title %>"
            class="img-fluid rounded"
            loading="lazy"
            width="1280" height="720"
          />
        </div>
        <div class="project-info">
          <h3 class="h4"><%= featured.title %></h3>
          <p class="mb-3"><%= featured.summary %></p>
          <a href="/projects/<%= featured.id %>" class="btn btn-outline-dark">View Project</a>
        </div>
      </div>
    <% } else { %>
      <p class="text-muted">No featured project available.</p>
    <% } %>
  </section>

  <!-- About Section -->
  <section class="about container my-5">
    <h2>About Me</h2>
    <p>
      I’m an artist exploring the intersection of design, technology, and culture. My work draws inspiration from streetwear aesthetics,
      digital art, and immersive experiences. Each piece tells a story about freedom, style, and creativity.
    </p>
  </section>

  <!-- Footer (Bootstrap) -->
  <footer class="bg-dark text-white text-center py-3 mt-5">
    <div class="container">
      <p class="mb-1">&copy; 2025 Quinn Harris | Personal NFT Mint</p>
      <a href="/contact" class="text-primary text-decoration-none">Contact Me</a>
    </div>
  </footer>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

  <!-- Optional site JS (served from /public/js) -->
  <script type="module" src="/js/index.js"></script>
</body>
</html>
